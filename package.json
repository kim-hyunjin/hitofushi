{
  "name": "hitofushi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "build:pages": "astro check && PUBLIC_BASE_PATH=/hitofushi/ PUBLIC_SITE_URL=https://kim-hyunjin.github.io astro build",
    "deploy": "npm run build:pages && gh-pages -d dist --nojekyll",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/preact": "^6.0.2",
    "astro": "^7.1.6",
    "preact": "^10.29.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "gh-pages": "^6.3.0",
    "typescript": "^6.0.3"
  }
}
